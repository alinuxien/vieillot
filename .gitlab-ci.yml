stages:
  - dependancies
  - test

dependancies:
  stage: dependancies
  tags:
    - shell
  script:
    - composer update || true
    - ls -la 
  artifacts:
    paths:
      - .
      
test:
  stage: test
  tags:
    - shell
  script:
    - cd dev
    - vendor/bin/phpunit

