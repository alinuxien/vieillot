stages:
  - dependancies
  - test

dependancies:
  stage: dependancies
  tags:
    - shell
  script:
    - composer update || true
  artifacts:
    paths:
      - .
      
test:
  stage: test
  tags:
    - shell
  script:
    - vendor/bin/phpunit

